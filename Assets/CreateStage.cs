using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreateStage : MonoBehaviour
{
    // public GameObject wall;
	// public GameObject moveWall;
	// public GameObject particalWall;
	// float timer =0.0f;
	// int interval=3;
	// float posy;
	// public float mainPos=4.0f;

	// private void Start() {
	// 	posy=wall.transform.position.y+mainPos;	
	// 	Debug.Log(posy);
	// }
	
	// void Update () {
	// 	timer+=Time.deltaTime;

	// 	if(timer >= interval){
	// 		int i=Random.Range(0,3);
	// 		// transform.position=new Vector3(0,Random.Range(posy-1.5f,posy+1.5f), transform.position.z);
	// 		transform.position=new Vector3(0, Random.Range(posy-1.0f, posy+1.0f), transform.position.z);
	// 		Debug.Log(posy);

	// 		if(i==0){
	// 			Instantiate(wall, transform.position, transform.rotation);
	// 		} else if(i==1){
	// 			Instantiate(moveWall, transform.position, transform.rotation);
	// 		} else if(i==2){
	// 			Instantiate(particalWall, transform.position, transform.rotation);
	// 		}
	// 		timer=0;

	// 	}

	// }
}
